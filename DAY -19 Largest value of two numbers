#include<stdio.h>
int main(){
    float x,y;
    printf("Enter any two numbers\n");
    scanf("%f %f",&x,&y);
    if(x>y)
     printf("Largest value is = % 0.2f\n",x);
    else
     printf("Largest value is = % 0.2f\n",y);
}
